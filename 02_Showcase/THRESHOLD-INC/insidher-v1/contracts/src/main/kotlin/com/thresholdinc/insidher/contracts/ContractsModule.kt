package com.thresholdinc.insidher.contracts

/**
 * Module marker for the :contracts module.
 *
 * This module contains all shared contract types (data models, sealed hierarchies,
 * JSON schemas, state machine definitions) used by :core and :android-app.
 *
 * It is a pure Kotlin/JVM module with no Android dependencies.
 */
object ContractsModule
